# Lightweight Requirements for Render Backend (NO ML - Uses Remote API)
# This version removes ALL ML dependencies to fit in Render's 512MB free tier
# ML inference is handled by Hugging Face Spaces

# ── Core Web Framework (Essential) ──────────────────────────────────────────
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# ── Database (Essential) ─────────────────────────────────────────────────────
sqlalchemy==2.0.23
psycopg2-binary==2.9.9

# ── Settings & Validation (Essential) ────────────────────────────────────────
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator
python-dotenv==1.0.0

# ── Authentication (Essential) ───────────────────────────────────────────────
python-jose==3.3.0
passlib[argon2]==1.7.4
firebase-admin==6.5.0

# ── HTTP Requests (Essential for calling Hugging Face API) ───────────────────
requests==2.31.0
httpx==0.25.2

# ── Basic Image Processing (Lightweight) ─────────────────────────────────────
pillow>=10.4.0

# ── Monitoring (Essential) ───────────────────────────────────────────────────
prometheus-client==0.19.0

# ── Utils ────────────────────────────────────────────────────────────────────
typing-extensions>=4.8.0

# ── System Monitoring (Optional) ──────────────────────────────────────────
psutil==5.9.6

# NOTE: NO PyTorch, scikit-learn, or heavy ML libraries!
# All ML inference is delegated to Hugging Face Spaces via HTTP API
